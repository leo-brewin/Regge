project check is

   for Object_Dir use "obj/";
   for Exec_Dir use ".";
   for Main use ("check.adb");

   package Compiler is
      for Default_Switches ("ada") use ("-gnatwA");
   end Compiler;

end check;
